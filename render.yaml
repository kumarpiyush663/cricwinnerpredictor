services:
  - type: web
    name: cricwinner-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/tournaments